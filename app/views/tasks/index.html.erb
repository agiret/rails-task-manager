<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <input type="checkbox" name="checkbox" <%= "checked" if task.completed %> >
      <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>
<div>
  <%= link_to "New task", new_task_path %>
</div>

